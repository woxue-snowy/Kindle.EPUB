# 第一章 || Chapter One

这是一本可以直接导入 Kindle 的示例电子书。 || This is a Kindle-ready sample book.
支持 **加粗**、*斜体* 和普通段落。 || It supports **bold**, *italic*, and regular paragraphs.

- 目录会自动生成
- The table of contents is generated automatically
- 中文元数据会写入 EPUB
- English metadata is included in the EPUB

# 第二章 || Chapter Two

把自己的 Markdown 文稿替换这个文件，就可以开始制作。 || Replace this Markdown file with your own manuscript to get started.